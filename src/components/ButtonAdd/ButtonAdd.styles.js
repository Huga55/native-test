import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonAdd: {
        paddingVertical: 6,
        alignItems: "center",
        backgroundColor: "#36d951",
    },
    buttonAddText: {
        fontSize: 18,
        color: "#fff",
    },
});